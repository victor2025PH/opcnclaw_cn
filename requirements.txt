# ============================================================
# OpenClaw v3.0 — 最小安装包依赖（云端模式，无需 GPU）
# ============================================================

# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# Audio processing (lightweight)
numpy>=1.26.0
soundfile>=0.12.1

# Cloud STT/TTS (zero-GPU)
edge-tts>=6.1.0
httpx>=0.26.0

# AI Backend
openai>=1.6.0

# Voice Activity Detection
silero-vad>=4.0.0

# Utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
loguru>=0.7.2
cryptography>=42.0.0

# Desktop
pystray>=0.19.5
customtkinter>=5.2.0
pillow>=10.0.0
jieba>=0.42.1

# QR code
qrcode[pil]>=7.4.2

# Desktop agent (OCR, optional)
pyperclip>=1.8.0
