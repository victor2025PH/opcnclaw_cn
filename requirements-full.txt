# ============================================================
# OpenClaw v3.0 — 完整安装包依赖（本地 GPU 模型）
# 包含最小安装包全部依赖 + 本地 STT/TTS 引擎
# ============================================================

-r requirements.txt

# Local STT — SenseVoice (Ali, 15x faster, emotion detection)
funasr>=1.0.0

# Local STT — Whisper fallback
faster-whisper>=1.0.0

# Deep Learning runtime
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.36.0

# Local audio processing (heavier)
librosa>=0.10.1
webrtcvad>=2.0.10

# Desktop vision control
rapidocr-onnxruntime>=1.2.0
mss>=9.0.0
pyautogui>=0.9.54

# Windows automation
uiautomation>=2.0.18

# Cloud TTS extras
elevenlabs>=1.0.0

# Dev
pytest>=7.4.0
pytest-asyncio>=0.23.0
black>=23.12.0
ruff>=0.1.0

# Local TTS (install from source):
# pip install -e CosyVoice/       # CosyVoice 2.0, voice cloning
# pip install -e fish-speech/      # Fish Speech, lightweight clone
